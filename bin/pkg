#!/usr/bin/env node

require("cyclops")
  .composeStore()
  .cyclops({
    argv: process.argv.slice(2),
    path: process.cwd(),
    task: "npm",
  })
  .catch(function(e) {
    console.error(e)
    process.exit(1)
  })
